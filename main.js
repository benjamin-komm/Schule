public class Main(){

	constuctor(){
		console.log("Server running on Port 3000");
		start(startus);
	}

	_stop(ref){
		Server.stop(null);
	}

	_start(ref){
		if(ref !== null){
			Server.start($status = ref);
		}
	}

	_createRef(onhashchange.onclick){
		while (onchange.length){
			case 0:
				return (releaseEvents(ref)) ? ip(v6) : ip(v4);
				break;
			case 1:
				retrun dhcp;
				break;
		}
	}

}
